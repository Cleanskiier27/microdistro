[project]
name = "microdistro"
version = "0.1.0"
description = "Starter network utility with Python and PowerShell entrypoints — Preciseliens Networkbuster Division, owned by Andrew Middlston"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
